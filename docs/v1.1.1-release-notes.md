# AgentsSessionQuery v1.1.1 发布说明

> 本文件为 v1.1.1 的发布说明（Release Notes），内容格式遵循 [Keep a Changelog](https://keepachangelog.com/en/2.0.0/) 2.0.0 约定（中文场景采用「英文（中文）」双语类型标题），版本号遵循语义化版本（SemVer）。
> 与 v1.1.1 的提交信息及 CHANGELOG.md 的 `[v1.1.1]` 段保持一致。

## [v1.1.1] - 2026-09-04

### Changed（变更）

- 精简 `.gitignore` 注释并解除对不存在文件的忽略：移除 `CodexActivityTools.psm1` 与 `NEATFREAK-AUDIT-*.md` 两条忽略规则（两文件本地均不存在，解除忽略无副作用），并删减冗余注释块；保留 `handoff/`（gitignored 本地私有）、`.agent_assets/`、`.workbuddy/` 等既有忽略项。

## 升级说明（含破坏性变更）

- 本版本**无破坏性变更**，亦无新增功能，属仓库维护性质更新，不影响 `asq` 运行时行为与查询结果。
- `.gitignore` 变更仅影响仓库忽略清单，不随工具分发内容变化；已安装用户无需任何操作即可沿用既有 `asq` 用法。
- 版本号说明：本次为 `.gitignore` 清理与注释精简，非破坏性、非新功能，按 SemVer 定级 PATCH（v1.1.1）；破坏性变更仍按既定策略升次版本（MINOR），预留 v2.x 给可视化前端开发线。
