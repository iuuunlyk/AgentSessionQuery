# AgentsSessionQuery v1.3.0 发布说明

> 本文件为 v1.3.0 的发布说明（Release Notes），内容格式遵循 [Keep a Changelog](https://keepachangelog.com/en/2.0.0/) 2.0.0 约定（中文场景采用“英文（中文）”双语类型标题），版本号遵循语义化版本（SemVer）。
> 与 v1.3.0 的提交信息及 CHANGELOG.md 的 `[1.3.0]` 段保持一致。

## [1.3.0] - 2026-09-17

### Added（新增）

- 新增 WorkBuddy 积分消耗（`Credits`）查询与统计展示：会话列表、`-c` / `-s` 详情与 `-AsJson` 输出均展示会话积分消耗合计，无积分数据以 `-`（JSON 为 `null`）表示。
