# AgentsSessionQuery v1.2.1 发布说明

> 本文件为 v1.2.1 的发布说明（Release Notes），内容格式遵循 [Keep a Changelog](https://keepachangelog.com/en/2.0.0/) 2.0.0 约定（中文场景采用“英文（中文）”双语类型标题），版本号遵循语义化版本（SemVer）。
> 与 v1.2.1 的提交信息及 CHANGELOG.md 的 `[1.2.1]` 段保持一致。

## [1.2.1] - 2026-09-10

### Fixed（修复）

- 澄清 `-d` / `-WithinDays` 非法值的报错文案（2026-09-10）：原文案结尾为 `收到「abc」` 式的双层引号，易被误读为标记或指令；现改为 `本次输入：abc`，并为关键字补充中文释义（`week` 本周周一起、`month` 本月 1 号起），同时把“天数小于 1”一支的措辞与“非法值”一支统一。仅影响提示文案，参数行为与退出码（`exit 1`）不变。
